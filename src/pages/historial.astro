---
import Base from '../layouts/Base.astro';
---

<Base titulo="Historial — Guía de Ejercicios" activa="historial">
  <h1>Historial</h1>
  <div class="carta" id="calendario"></div>
  <button class="boton-principal" id="btn-cardio">+ Registrar cardio</button>
  <div id="alta-cardio"></div>
  <h2>Sesiones</h2>
  <div id="aviso-historial"></div>
  <div id="sesiones"></div>
</Base>

<style>
  #calendario :global(.cabecera) { display: flex; justify-content: space-between; align-items: center; margin-bottom: 8px; }
  #calendario :global(.grilla) {
    display: grid;
    grid-template-columns: repeat(7, 1fr);
    gap: 4px;
    text-align: center;
  }
  #calendario :global(.dow) { font-size: 11px; color: var(--tinta-suave); font-weight: 700; }
  #calendario :global(.dia) {
    aspect-ratio: 1;
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: 50%;
    font-size: 13px;
    font-variant-numeric: tabular-nums;
  }
  #calendario :global(.dia.fuerza) { background: var(--fuerza); color: #fff; font-weight: 700; }
  #calendario :global(.dia.cardio) { background: var(--cardio); color: #fff; font-weight: 700; }
  #calendario :global(.dia.elongacion) { background: var(--elongacion); color: #fff; font-weight: 700; }
  #calendario :global(.dia.otro) { background: var(--accion); color: var(--accion-tinta); font-weight: 700; }
  #calendario :global(.dia.hoy) { outline: 2px solid var(--tinta); }
  /* Día con más de un tipo de sesión: el círculo se reparte entre los colores. */
  #calendario :global(.dia.multi) { color: #fff; font-weight: 700; }
  #calendario :global(.leyenda) { display: flex; gap: 10px; flex-wrap: wrap; margin-top: 10px; font-size: 12px; color: var(--tinta-suave); }
  #calendario :global(.punto) { display: inline-block; width: 10px; height: 10px; border-radius: 50%; margin-right: 4px; }

  #sesiones :global(details) {
    background: var(--carta);
    border: 1px solid var(--borde);
    border-radius: var(--radio);
    padding: 10px 14px;
    margin: 8px 0;
  }
  /* El tipo de sesión también se ve en la lista, no solo en el calendario. */
  #sesiones :global(details.tipo-fuerza) { border-left: 4px solid var(--fuerza); }
  #sesiones :global(details.tipo-cardio) { border-left: 4px solid var(--cardio); }
  #sesiones :global(details.tipo-elongacion) { border-left: 4px solid var(--elongacion); }
  #sesiones :global(summary) { font-weight: 700; cursor: pointer; }
  #sesiones :global(summary .tipo) { font-weight: 600; color: var(--tinta-suave); }
  #sesiones :global(.meta) { font-size: 13px; color: var(--tinta-suave); }
  #aviso-historial :global(.deshacer) { display: flex; align-items: center; gap: 10px; }
  #aviso-historial :global(.deshacer span) { flex: 1; }
  #aviso-historial :global(.deshacer button) { padding: 8px 14px; font-size: 14px; }
  #sesiones :global(.filtros) { display: flex; flex-wrap: wrap; align-items: center; gap: 8px; margin: 4px 0 12px; }
  #sesiones :global(.filtros .chip) { padding: 6px 12px; font-size: 13px; }
  #sesiones :global(.filtros select) { width: auto; flex: 1 1 160px; padding: 8px 10px; font-size: 14px; }
  #sesiones :global(.filtros .conteo) { width: 100%; margin: 0; font-variant-numeric: tabular-nums; }
  #sesiones :global([data-ver-mas]) { margin-top: 10px; }
  #sesiones :global(.acciones-sesion) { display: flex; gap: 8px; margin-top: 10px; }
  #sesiones :global(.acciones-sesion button) { flex: 1; font-size: 14px; padding: 9px; }
  #sesiones :global(.panel-editar) { margin-top: 10px; border-top: 1px solid var(--borde); padding-top: 10px; }
  #sesiones :global(.panel-editar textarea) { width: 100%; }
  #sesiones :global(.bloque-item) { margin: 8px 0; }
  #sesiones :global(.nota-item) { margin-top: 6px; font-size: 14px; }
  #sesiones :global(.serie-edit) {
    display: grid;
    grid-template-columns: 64px 1fr 1fr;
    gap: 8px;
    align-items: center;
    margin: 6px 0;
  }
</style>

<script>
  import { montarHistorial } from '../ui/historial';
  import { cargarCatalogoCompleto, hoyISO, exigirPerfil } from '../ui/datos';

  if (exigirPerfil()) {
    cargarCatalogoCompleto().then((catalogo) => {
      montarHistorial({
        raiz: document,
        catalogo,
        hoy: hoyISO,
        confirmar: (mensaje) => window.confirm(mensaje),
      });
    });
  }
</script>
